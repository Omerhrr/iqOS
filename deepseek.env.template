# iqOS Copilot - LLM provider config
# See .env.example in this folder for the full list of providers/options.

USE_ZAI=false
USE_DEEPSEEK=true
USE_ANTHROPIC=false
USE_OPENAI=false
USE_GEMINI=false
USE_OLLAMA=false

# Paste your DeepSeek API key here (from https://platform.deepseek.com/api_keys),
# then restart `bun index.ts`. Nothing else to change - the model defaults to
# deepseek-chat (override with DEEPSEEK_MODEL= if you want deepseek-reasoner).
DEEPSEEK_API_KEY=
